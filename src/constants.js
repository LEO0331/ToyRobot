// List allowed inputs
const Commands = ["PLACE", "MOVE", "LEFT", "RIGHT", "REPORT"];
const Directions = ["NORTH", "EAST", "SOUTH", "WEST"];
const Table = {
  min: 0,
  max: 5,
  size: 6,
};

export { Commands, Directions, Table };
